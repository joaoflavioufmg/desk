[build-system]
requires = ["setuptools>=60"]
build-backend = "setuptools.build_meta"

[project]
name = "desk-sim"
version = "1.0.2"
description = "DESK: Discrete Event Simulation Kit"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "GPL-3.0" }
authors = [
  { name = "João Flávio", email = "joao.flavio@dep.ufmg.br" }
]

keywords = [
  "discrete-event-simulation",
  "simulation",
  "operations-research",
  "healthcare",
  "logistics",
  "analytics"
]

classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Topic :: Scientific/Engineering",
  "License :: OSI Approved :: GPL-3.0 License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11"
]

dependencies = [
  "simpy>=4.1,<5",
  "numpy>=1.23,<2.1",
  "pandas>=1.5,<2.2",
  "scipy>=1.9,<1.12",
  "matplotlib>=3.6,<3.9",
  "seaborn>=0.12,<0.14"
]

[project.optional-dependencies]
dev = [
  "pytest",
  "pytest-cov",
  "black",
  "isort",
  "flake8",
  "build",
  "twine",
  "mkdocs",
  "mkdocs-material"
]

[project.urls]
Homepage = "https://joaoflavioufmg.github.io/desk"
Repository = "https://github.com/joaoflavioufmg/desk"
Issues = "https://github.com/joaoflavioufmg/desk/issues"

[tool.setuptools]
packages = { find = { exclude = ["tests*", "docs*", "examples*", "scripts*", "venv*"] } }

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]

[tool.black]
line-length = 88
target-version = ["py310"]

[tool.isort]
profile = "black"