name: Docs

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Upgrade pip
        run: |
          python -m pip install --upgrade pip

      - name: Install documentation dependencies
        run: |
          pip install -r docs/requirements.txt

      - name: Build MkDocs site
        run: |
          mkdocs build --strict

      # =========================
      # Upload to Read the Docs
      # =========================
      # - name: Upload docs to Read the Docs
      #   uses: readthedocs/actions/upload@v1
      #   with:
      #     api-token: ${{ secrets.RTD_API_TOKEN }}
      #     project: desk
      #     version: latest
      #     path: site/
      # - name: Trigger Read the Docs build
      #   run: |
      #     curl -X POST \
      #       -H "Authorization: Token ${{ secrets.RTD_API_TOKEN }}" \
      #       -H "Content-Type: application/json" \
      #       https://readthedocs.org/api/v3/projects/desk/versions/latest/builds/

  rtd:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Read the Docs build
        run: |
          curl -X POST \
            -H "Authorization: Token ${{ secrets.RTD_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            https://readthedocs.org/api/v3/projects/desk/versions/latest/builds/

      # # =========================
      # # GitHub Pages
      # # =========================
      # - name: Deploy to GitHub Pages
      #   run: |
      #     mkdocs gh-deploy --force
